

<div class="container">
  <h2>
    Archive
  </h2>
  <div class="row">
    <div class="col-xs-4">
      <div class="scrollbox">
        <div class="list">
          <% @movies.each do |movie| %>
          <div class="list-item">
           <%= link_to "#{movie.title}", movie_path(movie), class: "super-btn" %>
         </div>
         <% end %>
       </div>
     </div>
   </div>
   <div class="col-xs-6">
     <div class="review-index">
      <div class="content-review-index">
      </div>
    </div>
  </div>
</div>
</div>

<button class="next-button"> Next </button>

<!--
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <div class="title">
          <h1>
            All the Movies
          </h1>


        </div>
      </div>
    </div>
  </div> -->

  <%# if policy(Movie).create? %>
  <%#= link_to 'Add a Movie', new_movie_path, class: "btn btn-primary" %>
  <%# end %>
